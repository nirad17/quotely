import 'package:flutter/material.dart';

class SearchByTopic extends StatelessWidget {
  const SearchByTopic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Search by topic')),
    );
  }
}